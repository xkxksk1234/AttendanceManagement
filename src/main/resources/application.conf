app {
    name = "근태관리"
    # 사용자 홈 하위에 DB 생성: ~/.maemong/attendance/attendance.db
    dataDir = ${?APP_DATA_DIR}
    dataDir = "${user.home}/.maemong/attendance"
    dbPath = "${app.dataDir}/attendance.db"
}